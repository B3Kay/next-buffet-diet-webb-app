/** @type {import('next').NextConfig} */
const nextConfig = {

    images: {
        domains: ['plus.unsplash.com', 'picsum.photos', 'images.unsplash.com', 'images.pexels.com', "cdn.pixabay.com"],

    }
}
export default nextConfig;